import { styled } from '@mui/material/styles';
import { CommonButtonStyled } from '../CommonButtonStyled';
export const ModalButton = styled(CommonButtonStyled)`
  &.MuiButton-root {
    color: var(--primary-green);
    border-color: var(--primary-green);
  }
`;
